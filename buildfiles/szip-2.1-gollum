# vim: set ft=sh:
pkgname=szip
pkgver=2.1
baseurl=https://www.hdfgroup.org/ftp/lib-external/szip/${pkgver}/src
pkgurl=${baseurl}/szip-${pkgver}.tar.gz

pkgmodule=$(cat <<END_HEREDOC
#%Module
set SZIP_DIR $destdir
setenv SZIP_DIR \$SZIP_DIR

append-path LD_LIBRARY_PATH \$SZIP_DIR/lib
END_HEREDOC
)
