# vim: set ft=sh:
pkgname=embree
pkgver=2.11.0
pkgurl=https://github.com/embree/embree/archive/v${pkgver}.tar.gz

export USE_CMAKE=1

pkgmodule=$(cat <<END_HEREDOC
#%Module
set EMBREE_DIR $destdir
setenv EMBREE_DIR \$EMBREE_DIR

prepend-path LD_LIBRARY_PATH \$EMBREE_DIR/lib64
END_HEREDOC
)

